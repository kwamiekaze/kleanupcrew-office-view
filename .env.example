# Server-only settings. Never prefix secrets with VITE_.
# Keep disabled until the sender domain and production challenge are tested.
QUOTE_DELIVERY_ENABLED=false
# Optional: comma-separated extra recipients. Both owner inboxes are built in.
QUOTE_TO_EMAIL=
QUOTE_FROM_EMAIL=
QUOTE_SITE_ORIGIN=
RESEND_API_KEY=
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
